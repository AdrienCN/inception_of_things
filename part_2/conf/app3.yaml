#Version du kubernetesAPI used
apiVersion: apps/v1
#type of Kubernetes object (Pods, Service, deployment ..)
kind: Deployment
#Data that identifies the OBJECT
    #name= Objectname
    #UID (Optional)
    #namespace (Optional)
    #labels = (Optional?) map of key:value (string) to organize scopes
metadata:
  name: bluewhale-deploy
  #labels:
  #  app: hellokubernetes
#Data about all the PODS
#(required) Spec = desired state of the object.
spec:
#Replicas = nb of similar pods to create
  replicas: 1
  selector:
    matchLabels:
      app: bluewhale-pod
#(required) Template = describe the pods 
  template:
    metadata:
      labels:
        app: bluewhale-pod
#POD spec
    spec:
      containers:
      - name: bluewhale-container
        image: hcwxd/blue-whale
        ports:
        - containerPort: 3000
